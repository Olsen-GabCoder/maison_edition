// src/environments/environment.ts
export const environment = {
    production: false,
    // ===> VÉRIFIEZ ET ADAPTEZ CETTE URL <===
    apiUrl: 'http://localhost:3000/api', // Exemple: URL du backend local
    // Assurez-vous que cette URL pointe vers la base de votre API (ex: avant /books)
  };